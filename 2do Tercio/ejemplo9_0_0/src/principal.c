#include "../include/ejemplo.h"

void main (void)
{
	float valor1 = 13.5, valor2 = 10.1, resultado = 0;
	
	resultado = sumar_valores(valor1, valor2);
	mostrar_resultado(resultado);
	restar_valores(valor1, valor2, &resultado);
	mostrar_resultado(resultado);
		int result = MULTIPLY(valor1 + 5);
		
			char *str1 = "Hello world";
	char *str2 = "Not so fast";

//	int minCharSum = MIN(sum_chars(str1), sum_chars(str2));
//	printf("MIN CHARS: %d\n", minCharSum);
 
}
